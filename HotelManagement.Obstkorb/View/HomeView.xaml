<UserControl x:Class="HotelManagement.Obstkorb.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid>
        <DataGrid ItemsSource="{Binding RoomStatusList}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Zimmernummer" Binding="{Binding RoomID}" Width="*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding BookingStatus}" Width="*"/>
                <DataGridTemplateColumn Header="Farbe" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding BookingStatus, Converter={StaticResource BookingStatusToColorConverter}}" Width="100" Height="25"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Meine Buchungen anzeigen" 
                Command="{Binding ShowBookingOverviewCommand}" 
                VerticalAlignment="Bottom"/>
    </Grid>
</UserControl>