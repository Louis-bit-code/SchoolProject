<Window x:Class="HotelManagement.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:HotelManagement"
        xmlns:viewModel="clr-namespace:HotelManagement.Obstkorb.ViewModel"
        Title="Login" Height="300" Width="400">
    <Window.DataContext>
        <viewModel:LoginViewModel />
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="Sign In" Command="{Binding ShowSignInViewCommand}" Grid.Row="0" Width="150" Margin="5"/>
        <Button Content="Sign Up" Command="{Binding ShowSignUpViewCommand}" Grid.Row="1" Width="150" Margin="5"/>

        <ContentControl Content="{Binding CurrentView}" Grid.Row="2" Margin="5"/>
    </Grid>
</Window>
